name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: JVM + CLJS + lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache m2 / gitlibs
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2
            ~/.gitlibs
          key: ${{ runner.os }}-clj-${{ hashFiles('deps.edn') }}
          restore-keys: ${{ runner.os }}-clj-

      # zero-dep portable .cljc lib — the CLJS gate is PRIMARY (fleet runtime
      # priority), the JVM one is the compat gate; both run the same suite.
      - name: JVM tests (compat gate)
        run: clojure -M:test

      - name: CLJS tests (primary gate — real ClojureScript, node target)
        run: clojure -Sdeps '{:paths ["src" "test"]}' -M:cljs -m cljs.main --target node -m merkle-sum.cljs-runner

      - name: Lint (clj-kondo, errors fail)
        run: clojure -M:lint
